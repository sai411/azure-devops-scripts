# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

parameters:
  - name: csh
    displayName: "Environment Variable for CX Search"
    type: string
    default: "qa05"
pool:
  name: aws

steps:
- script: bash file.sh
  displayName: 'Run a one-line script'